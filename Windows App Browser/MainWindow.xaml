<Window
    x:Class="WindowsAppBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Windows_App_Browser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="Windows App Browser">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Back" Click="OnBackButtonClick" IsEnabled="{Binding IsBackEnabled}"/>
            <Button Content="Refresh" Click="OnRefreshButtonClick" IsEnabled="{Binding IsRefreshEnabled}"/>
            <TextBox x:Name="addressBar" Width="300" PlaceholderText="输入地址" Text="{Binding Address}"/>
            <Button Content="选项" Click="OnOptionsButtonClick"/>
        </StackPanel>
        <controls:TabView x:Name="tabView" Grid.Row="1" SelectionChanged="OnTabSelectionChanged">
            <controls:TabView.TabItems>
                <controls:TabViewItem Header="Home Page">
                    <Grid>
                        <!-- 在这里添加首页内容 -->
                    </Grid>
                </controls:TabViewItem>

            </controls:TabView.TabItems>
        </controls:TabView>
    </Grid>
</Window>
